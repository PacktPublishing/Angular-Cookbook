import { PopoverPositionalClassDirective } from './popover-positional-class.directive';

describe('PopoverPositionalClassDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverPositionalClassDirective();
    expect(directive).toBeTruthy();
  });
});
